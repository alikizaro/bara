# تقرير تسليم مشروع «برا السالفة» إلى Codex

**تاريخ اللقطة:** 7 أغسطس 2026  
**المنصة المستهدفة الآن:** Android  
**حالة المشروع:** تطبيق React Native/Expo كامل وظيفيًا على مستوى الكود، لكنه لم يُربط بعد بخدمات سحابية دائمة، ولم يُبنَ APK نهائي، ولم يُختبر بين أجهزة حقيقية.

## 1. تعليمات البدء إلى Codex

أنت تستلم مشروع لعبة عربية جماعية اسمها **«برا السالفة»**. المطلوب هو **متابعة المشروع الحالي وعدم إعادة بنائه من الصفر**.

قبل تعديل أي كود:

1. اقرأ `AGENTS.md` و`README.md` كاملين.
2. افحص الملفات الحالية وشغّل الفحوص الأساسية.
3. لا تستخدم `git reset --hard` أو `git restore` أو أي أمر قد يحذف هذه اللقطة؛ النسخة الأصلية احتوت تغييرات غير مسجلة، والـZIP هو مصدر الحقيقة الحالي.
4. لا ترقي Expo أو React Native أو LiveKit تلقائيًا. ثُبّت المشروع على Expo SDK 56 لأن الصوت الأصلي عبر LiveKit/WebRTC جرى ضبطه لهذه التوليفة. اقرأ توثيق Expo 56 المحدد في `AGENTS.md` قبل أي تعديل.
5. حافظ على البنية المعيارية: الواجهات في `src/`، منطق الخادم في `convex/`، وكتالوج المحتوى في `convex/data/catalog/`.
6. لا تضع مفاتيح LiveKit أو أي أسرار داخل التطبيق أو Git أو المحادثة. مفاتيح LiveKit يجب أن تبقى في بيئة Convex فقط.
7. اطلب من المستخدم فقط خطوات تسجيل الدخول التفاعلية التي يجب أن تكون باسم حسابه. لا تطلب كلمة مرور أو مفتاحًا سريًا داخل الدردشة.

## 2. الهدف المباشر

الهدف التالي ليس إضافة مزايا شكلية، بل الوصول إلى **APK حقيقي يعمل أونلاين بين عدة هواتف Android**:

1. ربط المشروع بحساب Convex دائم ونشر المخطط والدوال.
2. ربط LiveKit Cloud بالصوت عبر أسرار محفوظة في Convex.
3. ربط المشروع بحساب Expo/EAS الخاص بالمستخدم.
4. بناء Development Build ثم APK تجريبي `preview`.
5. اختبار اللعبة من البداية إلى النهاية على ثلاثة هواتف Android على الأقل.
6. إصلاح مشاكل الاستئناف والانقطاع الموضحة في قسم «الأولويات قبل التوزيع».
7. تسليم APK قابل للتثبيت مع نسخة مصدر محدثة.

لا تعتبر المهمة مكتملة بمجرد نجاح البناء؛ يجب إثبات أن الغرف والصوت والتصويت والجولة التالية تعمل بين أجهزة فعلية.

## 3. فكرة اللعبة والقواعد المعتمدة

اللعبة جماعية أونلاين من **3 إلى 10 لاعبين**:

1. يدخل كل لاعب باسم عرض فقط.
2. ينشئ المضيف غرفة، ويختار الصنف والإعدادات، ثم يرسل رمزًا من 6 خانات.
3. يدخل الأصدقاء بالرمز ويعلنون الاستعداد.
4. يختار الخادم عنصرًا سريًا ولاعبًا واحدًا عشوائيًا ليكون **«برا السالفة»**.
5. جميع اللاعبين داخل السالفة يرون اسم العنصر وصورته، أما برا السالفة فلا يراهما.
6. تبدأ مرحلة الأسئلة التلقائية: اللعبة تختار السائل والمجيب تلقائيًا لكل حوار.
7. تبدأ بعدها مرحلة الأسئلة الحرة: يأخذ كل لاعب دوره ويختار بنفسه لاعبًا آخر ليسأله.
8. بعد انتهاء جميع الأسئلة يصوّت كل لاعب لشخص آخر يعتقد أنه برا السالفة.
9. بعد اكتمال التصويت، يحصل برا السالفة دائمًا على محاولة تخمين من أربعة خيارات، حتى إن لم يحصل على أعلى عدد من الأصوات.
10. تظهر النتيجة، نقاط الجولة، والمجموع التراكمي داخل الغرفة، ثم يبدأ المضيف جولة جديدة.

### إعدادات الغرفة

- الصنف: 8 أصناف موضحة أدناه.
- عدد اللاعبين: من 3 إلى 10.
- عدد الأسئلة التلقائية الإجمالي: من 1 إلى 10.
- عدد الأسئلة الحرة لكل لاعب: من 1 إلى 3.

### نظام النقاط الحالي

- كل لاعب يصوّت لبرا السالفة الصحيح يحصل على نقطة واحدة.
- برا السالفة يحصل على نقطة واحدة إذا اختار العنصر الصحيح من الخيارات الأربعة.
- النقاط تتراكم داخل الغرفة، ويوجد أيضًا `totalPoints` مرتبط بهوية تثبيت التطبيق.

لا تغيّر هذه القواعد دون طلب صريح من المستخدم.

## 4. المحتوى الموجود

الأصناف الحالية:

1. الحيوانات
2. الأنمي
3. الدول
4. المدن
5. الأكلات
6. المشاهير
7. كرة القدم
8. منوّع

مكتبة الأنمي تضم 12 حزمة مستقلة بالإضافة إلى «كل الأنميات»، وبها **1,078 اسم شخصية فريد**. حزمة ناروتو منفصلة عن بوروتو، ويوجد اختبار يمنع دخول شخصيات بوروتو إلى ناروتو.

مكتبة الحيوانات تضم **168 حيوانًا و168 صورة**. جرى ربط 167 صورة بصفحات إنجليزية محددة، وصورة «عصفور» بصفحتها العربية الصحيحة. هذا الإصلاح أُضيف لمنع أخطاء قديمة مثل ظهور صورة كلب للثعلب أو درج للغزال.

الملفات الأساسية للمحتوى:

- `convex/data/catalog/core_words.ts`
- `convex/data/catalog/anime_packs.ts`
- `convex/data/catalog/core_artwork.generated.json`
- `convex/data/catalog/anime_artwork.generated.json`
- `convex/data/catalog/animal_wikipedia_pages.ts`
- `convex/data/catalog/catalog.ts`
- `convex/data/catalog/catalog.test.ts`

العناصر التي تدخل القرعة تستخدم الصور المراجعة عندما تتوفر أربعة عناصر مصورة على الأقل. الصور تُحمّل عن بعد، وعند فشلها تعرض الواجهة رمزًا بديلًا بدل صورة خاطئة.

## 5. البنية التقنية الحالية

### التطبيق

- React Native `0.85.3`
- React `19.2.3`
- Expo SDK `~56.0.19`
- TypeScript strict
- واجهة عربية RTL وداكنة
- ليس WebView؛ التطبيق أصلي عبر React Native

### الخادم والمزامنة

- Convex `^1.43.0`
- يحفظ اللاعبين والغرف والعضويات والجولات والأصوات والنقاط.
- يستعمل الاشتراكات اللحظية لمزامنة الغرفة وحالة الجولة بين الأجهزة.
- العنصر السري ودور برا السالفة محميان في استجابة الخادم؛ كل لاعب يستلم العرض المسموح له فقط.
- هوية الضيف هي UUID محفوظ في `expo-secure-store` باسم تثبيت التطبيق.

### الصوت

- LiveKit React Native + WebRTC أصلي.
- `registerGlobals()` يُستدعى في `index.ts` قبل تشغيل التطبيق.
- رمز LiveKit يصدر من `convex/livekit.ts`، والمفاتيح لا تدخل APK.
- كل اللاعبين يشتركون في الصوت، والواجهة تفتح الميكروفون للسائل والمجيب الحاليين فقط.
- الصوت يحتاج Development Build أو APK أصلي، ولن يعمل داخل Expo Go.

### أهم المسارات

| المسار | المسؤولية |
| --- | --- |
| `App.tsx` | التنقل البسيط بين الدخول، الرئيسية، إنشاء/دخول الغرفة، الانتظار، واللعبة |
| `src/screens/` | شاشات التطبيق |
| `src/features/game/` | الدور السري، الأسئلة، التصويت، التخمين، والنتيجة |
| `src/features/voice/` | الاتصال بـLiveKit والتحكم بالمايكروفون |
| `src/state/online-game-provider.tsx` | ربط واجهة React بدوال Convex |
| `src/state/demo-game-provider.tsx` | معاينة محلية عند غياب رابط Convex |
| `src/domain/` | الأنواع وقواعد الواجهة |
| `convex/schema.ts` | جداول قاعدة البيانات وفهارسها |
| `convex/rooms.ts` | إنشاء/دخول/استعداد/بدء/مغادرة الغرفة |
| `convex/game.ts` | مراحل الجولة والتصويت والتخمين والنقاط |
| `convex/players.ts` | هوية الضيف والاسم والنبض |
| `convex/livekit.ts` و`convex/voiceAuth.ts` | إصدار رمز الصوت والتحقق من العضوية |
| `convex/lib/` | الصلاحيات والسياسات واختيار الجولة وتجهيز عرض الغرفة |
| `app.json` | حزمة Android والصلاحيات والإضافات الأصلية |
| `eas.json` | ملفات Development وAPK Preview وAAB Production |

## 6. إعداد Android الحالي

- اسم الحزمة: `com.aliwendy.baralsalfa`
- الاتجاه: عمودي.
- الصلاحيات: الإنترنت، تسجيل الصوت، وتعديل إعدادات الصوت.
- الكاميرا محجوبة صراحة لأن اللعبة لا تحتاج إليها.
- توجد أيقونة أصلية في `assets/bara-alsalfa-icon.png`.
- ملف `preview` في `eas.json` يبني APK داخليًا.
- ملف `production` يبني AAB لمتجر Google Play.
- لا يوجد حتى الآن `extra.eas.projectId`؛ يجب إنشاء/ربط مشروع EAS باسم المستخدم.

## 7. وضع التشغيل التجريبي مقابل الأونلاين

`src/state/game-backend-provider.tsx` يفحص `EXPO_PUBLIC_CONVEX_URL`:

- إذا كان الرابط صحيحًا ويبدأ بـ`https://`، يستخدم `OnlineGameProvider` ويعمل عبر Convex.
- إذا كان المتغير مفقودًا، يستخدم `DemoGameProvider` ويعرض لاعبين وهميين على جهاز واحد.

توجد لافتة داخل الواجهة تبيّن الوضع، لكن **يُمنع تسليم APK للأصدقاء قبل التأكد أنه بُني بمتغير Convex الصحيح**. يفضّل إضافة حاجز إصدار يمنع بناء `preview` أو `production` في وضع Demo بدل الاعتماد على اللافتة فقط.

## 8. الحالة المثبتة بتاريخ التسليم

نُفذت الفحوص التالية بنجاح في 7 أغسطس 2026:

- `npm run typecheck`: نجح.
- `vitest run`: نجحت **10/10 اختبارات** في 3 ملفات.
- `eslint .`: نجح.
- `expo-doctor`: نجحت **21/21 فحوص**.
- `expo export --platform android`: نجح، وMetro جمع 943 وحدة وأنتج حزمة Hermes Android بحجم يقارب 4.1 MB.

هذه النتائج تثبت سلامة TypeScript والحزمة، لكنها **لا تثبت** النشر السحابي أو الصوت الحقيقي أو اللعب بين الأجهزة.

الحالة غير المنجزة:

- لا يوجد `.env.local` مربوط بخادم Convex.
- لا يوجد نشر Convex سحابي دائم باسم المستخدم.
- لم تُحفظ قيم LiveKit في بيئة Convex.
- لم يُربط المشروع بحساب Expo/EAS.
- لا يوجد APK نهائي حاليًا.
- لم يُجر اختبار شامل على جهازين أو ثلاثة أجهزة فعلية.
- ملفات `convex/_generated/` موجودة بصيغة عامة `AnyApi` ويجب إعادة توليدها بعد أول `convex dev` سحابي ناجح.

## 9. خطة الربط والبناء

نفّذها بالتتابع وتحقق بعد كل مرحلة:

### أ. تهيئة المصدر

1. فك الضغط وادخل مجلد المشروع.
2. ثبّت النسخ المقفلة بـ`npm ci`.
3. شغّل `npm run verify` و`npx expo-doctor`.
4. أنشئ نقطة حفظ Git أولى بعد التأكد من أن اللقطة كاملة، من دون حذف أي ملف حالي.

### ب. ربط Convex

1. سجّل دخول المستخدم تفاعليًا إلى Convex.
2. شغّل `npx convex dev` لإنشاء أو ربط مشروع دائم.
3. تأكد أن الأمر يولد أنواع `convex/_generated/` الحقيقية ويكتب `EXPO_PUBLIC_CONVEX_URL` في `.env.local`.
4. عالج أي خطأ يرفض المخطط أو الدوال، ثم أعد `npm run verify`.
5. انشر نشر الإنتاج بـ`npx convex deploy` عندما تصبح بيئة التطوير سليمة.

### ج. ربط LiveKit

1. ينشئ المستخدم مشروع LiveKit Cloud باسمه.
2. احفظ `LIVEKIT_URL` و`LIVEKIT_API_KEY` و`LIVEKIT_API_SECRET` في بيئة Convex للتطوير والإنتاج.
3. لا تضع هذه القيم في `.env.local` الخاصة بالتطبيق ولا في EAS.
4. اختبر إصدار رمز الصوت من الخادم قبل بناء APK.

### د. ربط Expo/EAS

1. سجّل دخول المستخدم إلى Expo/EAS تفاعليًا.
2. اربط/أنشئ مشروع EAS، وأضف `projectId` الناتج بالطريقة الرسمية.
3. تأكد من أن اسم الحزمة `com.aliwendy.baralsalfa` متاح ومملوك للمستخدم؛ لا تغيّره دون تنبيهه.
4. عرّف `EXPO_PUBLIC_CONVEX_URL` لبيئتي `preview` و`production` بالقيمة المناسبة.
5. ابنِ Development Build أولًا لاختبار الصوت، ثم ابنِ APK بملف `preview`.

### هـ. الاختبار الفعلي

استخدم ثلاثة أجهزة Android على الأقل وحساب/تثبيت مستقل في كل جهاز. اختبر:

- إنشاء الغرفة والدخول بالرمز.
- منع بدء اللعبة بأقل من 3 لاعبين أو قبل استعداد الجميع.
- سرية العنصر ودور برا السالفة على الأجهزة المختلفة.
- الأسئلة التلقائية والانتقال الصحيح بينها.
- دور كل لاعب في الأسئلة الحرة واختيار المجيب.
- فتح مايك السائل والمجيب وسماع الجميع لهما.
- رفض إذن المايكروفون ثم منحه من الإعدادات.
- التصويت ومنع التصويت للنفس وتغيير التصويت قبل اكتمال الجميع.
- انتقال برا السالفة دائمًا إلى خيارات التخمين الأربعة.
- صحة النقاط وجدول الترتيب.
- بدء جولة ثانية وعدم تكرار العنصر أو برا السالفة مباشرة عندما توجد بدائل.
- صور الثعلب والغزال وعينة من بقية الحيوانات.
- مغادرة المضيف واللاعب العادي، انقطاع الإنترنت، إغلاق التطبيق، ثم العودة.

## 10. الأولويات قبل توزيع APK للأصدقاء

هذه ليست أخطاء TypeScript، بل حالات تشغيل حقيقية يجب حلها أو قبولها بوضوح:

### أولوية حرجة

1. **استئناف الغرفة بعد إغلاق التطبيق:** `roomId` محفوظ حاليًا في React state فقط. إذا قُتل التطبيق أو أُعيد تشغيله، لا يستعيد اللاعب الغرفة، كما أن الانضمام الحالي يرفض الغرفة بعد بدء المباراة. احفظ العضوية النشطة/استرجعها من الخادم واسمح بإعادة الاتصال الآمنة.
2. **اللاعب المنقطع قد يجمّد الجولة:** نبض الاتصال يغيّر `isOnline` بصريًا فقط، لكنه لا يزيل العضوية. لاعب غائب قد يمنع اكتمال التصويت أو يكون طرفًا في سؤال. أضف للمضيف آلية آمنة للتخطي/الإزالة أو سياسة مهلة واضحة.
3. **تأكيد بيئة الإصدار:** امنع بناء أو توزيع `preview/production` إذا كان `EXPO_PUBLIC_CONVEX_URL` مفقودًا حتى لا يحصل المستخدم على نسخة Demo بالخطأ.

### أولوية عالية

4. **صلاحيات النشر الصوتي:** رمز LiveKit الحالي يمنح `canPublish: true` لكل عضو، بينما منع الكلام يُطبق في العميل عبر إغلاق المايك. هذا مناسب للنموذج الأولي لكنه ليس حماية خادمية ضد عميل معدل. راجع تحديث صلاحيات المشاركين أو إصدار تصريح متوافق مع الدور الحالي قبل النشر العام.
5. **انتهاء رمز LiveKit:** مدة الرمز ساعتان ولا توجد آلية واضحة لتحديثه تلقائيًا أو إعادة الاتصال بعد انتهائه.
6. **المضيف المنقطع:** المغادرة الصريحة تنهي الغرفة، لكن الانقطاع المفاجئ لا ينقل الاستضافة ولا ينهيها. قرر هل تُنقل الاستضافة أم تُمنح مهلة عودة.
7. **تنظيف الغرف القديمة:** لا توجد مهمة دورية لحذف/أرشفة الغرف المنتهية والجولات القديمة.

### تحسينات لاحقة

8. الصور عن بعد ولا توجد طبقة تخزين مؤقت/تنزيل مسبق؛ اختبر الأداء على شبكة عراقية بطيئة.
9. هوية اللاعب مرتبطة بتثبيت التطبيق فقط؛ حذف التطبيق يفقد الوصول إلى النقاط السابقة. يمكن إضافة حساب اختياري لاحقًا، لكن ليس شرطًا لأول APK خاص بالأصدقاء.
10. التنقل الحالي يدوي في `App.tsx`. لا تغيّره قبل إنهاء الربط والاختبار إلا إذا ظهر عطل فعلي.

## 11. اعتبارات الأمان والخصوصية

- لا تكشف `secretName` أو `secretImageUrl` للاعب برا السالفة قبل النتائج.
- لا تعتمد على إخفاء واجهة فقط؛ جميع الانتقالات والتصويت والنقاط يجب أن تبقى محمية في Convex.
- أبقِ مفاتيح LiveKit في Convex فقط.
- لا تسجل رموز LiveKit أو معرف تثبيت اللاعب في سجلات عامة.
- لا تضف صلاحية الكاميرا.
- افحص أن العضو ينتمي إلى الغرفة في كل Mutation/Action جديدة.
- لا تسمح للعميل بتحديد النقاط أو الدور السري أو نتيجة التخمين.

## 12. تعريف الإنجاز للمرحلة التالية

تُعتبر مرحلة Android التجريبية منجزة فقط عند تحقق كل الآتي:

- Convex منشور دائمًا بحساب المستخدم.
- LiveKit مربوط ويعمل من دون وجود الأسرار في APK.
- APK `preview` مرتبط بالخادم الحقيقي وليس Demo.
- ثلاثة هواتف تكمل جولة كاملة بالصوت والتصويت والتخمين والنتائج والجولة التالية.
- إعادة فتح التطبيق لا تفقد الغرفة النشطة.
- المضيف يستطيع معالجة لاعب منقطع من دون تجميد المباراة.
- جميع الفحوص الحالية ما زالت ناجحة.
- تسليم رابط/ملف APK ونسخة مصدر محدثة وتقرير مختصر بما تغير.

## 13. الأمر المقترح لبدء جلسة Codex

يمكن للمستخدم إرسال النص التالي بعد رفع ملف المشروع:

> فك ضغط المشروع واقرأ `CODEX_HANDOFF_AR.md` و`AGENTS.md` و`README.md` كاملًا. تابع المشروع الحالي ولا تبدأ من الصفر. هدفك الأول هو ربط Convex وLiveKit وExpo/EAS بحساباتي، ثم معالجة استعادة الغرفة واللاعب المنقطع، وبناء APK تجريبي واختباره بين ثلاثة أجهزة Android. لا تطلب مني كلمات مرور أو مفاتيح داخل الدردشة؛ استخدم تسجيل الدخول التفاعلي، وتوقف فقط عندما تحتاج مني إتمام خطوة ملكية حساب. حافظ على Expo SDK 56 والبنية المعيارية، وشغّل الفحوص بعد كل تعديل مهم.

